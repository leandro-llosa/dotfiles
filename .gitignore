# --- Secrets / per-machine state (must never enter the repo) -----------------
.env
.env.local
*.env.local

# Files rendered from *.tmpl by scripts/render-templates.sh. The .tmpl is
# tracked; the rendered file is not (it embeds machine-specific secrets).
git/.gitconfig
claude-code/.claude/settings.zc.json
codex/.codex/config.toml

# --- Editor / OS noise -------------------------------------------------------
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# --- Build artifacts (kept from the previous incarnation of this repo) -------
luac.out
*.src.rock
*.zip
*.tar.gz
*.o
*.os
*.ko
*.obj
*.elf
*.gch
*.pch
*.lib
*.a
*.la
*.lo
*.def
*.exp
*.dll
*.so
*.so.*
*.dylib
*.exe
*.out
*.app
*.i*86
*.x86_64
*.hex
